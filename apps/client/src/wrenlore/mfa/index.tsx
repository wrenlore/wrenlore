import { Alert } from "@mantine/core";

export function MfaSettings() {
  return (
    <Alert variant="light" color="gray">
      Multi-factor authentication is planned as a WrenLore-native module.
    </Alert>
  );
}
